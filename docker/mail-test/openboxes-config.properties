# Test-only external config for verifying mail delivery against a local SMTP
# sink (MailHog). Mounted into the app container by docker-compose.mail-test.yml.
# Do NOT use in production; production mail stays disabled by default.
grails.mail.enabled=true
grails.mail.host=mailhog
grails.mail.port=1025
