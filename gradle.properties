org.gradle.caching=true
org.gradle.console=plain
org.gradle.daemon=true
org.gradle.parallel=false
org.gradle.warning.mode=all

# the version of our application. Grails can't seem to find it in build.gradle
version=0.9.9-SNAPSHOT

# 7.1+ is required for Java 11+ classfile support
asmVersion=7.3.1
# logging mysteriously broke in 3.2.4+
assetPipelineVersion=3.2.3
batikVersion=1.17
#
# Set this to 1.44 to work with Docker Engine 29 (Nov 2025).
# https://github.com/testcontainers/testcontainers-java/pull/11216
#
dockerApiVersion=1.44
docx4jVersion=8.3.8
flyingSaucerVersion=9.1.22
gradleWrapperVersion=6.9.4
grailsVersion=4.1.4
grailsViewsVersion=2.0.4
grailsWrapperVersion=1.0.0
groovyVersion=2.5.14
htmlUnitVersion=2.70.0
httpComponentsVersion=4.5.14
httpCoreVersion=4.4.16
# managed by Spring Boot 2.2.13 (Grails 4.1); databind carries an extra hotfix digit
jacksonVersion=2.10.5
jacksonDatabindVersion=2.10.5.1
# grails-core pulls in 4.2.2 via directory-watcher. This breaks
# Macs, which need 5.8.0+, and testcontainers, which requests 5.13+.
# JNA's only breaking changes between 4.2.2 and today remove APIs
# that directory-watcher does not use.
# https://github.com/java-native-access/jna/blob/master/CHANGES.md#release-580
# https://github.com/java-native-access/jna/blob/master/CHANGES.md#breaking-changes
# https://github.com/java-native-access/jna/blob/master/CHANGES.md#breaking-changes-1
jnaVersion=5.13.0
# Grails's Spring Boot dependency doesn't play well with Logback 1.3+
logbackVersion=1.2.13
# the version managed by Spring Boot 2.2.13 (Grails 4.1)
tomcatVersion=9.0.41
# use the version that Flying-Saucer-PDF was tested and released against
# https://github.com/flyingsaucerproject/flyingsaucer/blob/v9.1.22/flying-saucer-pdf/pom.xml
openPdfVersion=1.3.11
owaspVersion=7.4.4
poiVersion=3.17
seleniumVersion=3.141.59
sentryVersion=6.34.0
servletVersion=4.0.1
slf4jProvidedLog4j2Version=2.19.0
slf4jVersion=1.7.36
springframeworkVersion=5.2.21.RELEASE
testContainersVersion=1.21.3
xDocReportVersion=2.0.4

#
# The following modules are tightly coupled. The grails-database-migration
# plugin documents the specific Liquibase version we need here:
#
# https://grails.github.io/grails-database-migration/3.1.0/index.html
#
# database-migration 3.1.x is the Grails 3/4 line (4.x targets Grails 5 and
# jumps to Liquibase 4.6, which drops legacy 1.9-XSD tags like modifyColumn
# that our historical changelogs still use), so we stay on Liquibase 3.10.1
# to keep changelog behavior identical.
#
# As for the mysql connector, version 8.0.23's API changes break
# Liquibase <=3.10.3 (https://github.com/liquibase/liquibase/issues/1639).
#
# N.B. the 8 here refers to Java's release, not to MySQL's: this
# is the correct library to use to access MySql 5.7+.
#
databaseMigrationVersion=3.1.0
gormVersion=7.0.9
# the gorm-hibernate5 line paired with GORM 7.0.x (see grails-bom 4.1.4)
gormHibernateVersion=7.0.5
hibernateVersion=5.4.33.Final
liquibaseVersion=3.10.1
mySqlConnectorVersion=8.0.22

# groovyc needs extra heap to compile the full test suite in one pass
org.gradle.jvmargs=-Xmx2g
